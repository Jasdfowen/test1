//erstellt unter Zusammenarbeit von Lukas Ströbel und Conrad Ackermann
/*#include <stdio.h>
#include <string.h>

typedef struct{
    int vorkomma;
    double nachkomma;
}kommazahl;


int gibKommaZahlEin(const char prompt[], kommazahl ergebnis)
{
    printf("%s", prompt);
    int eingelesen = 1;
    while(eingelesen > 0){
        char eingelesen = 0;
        scanf("%d,%d", eingabe);
    }
}
addiereNachKommaStellen(int nachkommaZahl, int Summand){
    
}
int main(void){
    kommazahl ergebnis = {0, 0};
    char prompt[] = "Gib Deine Kommazahlen ein (Abschluss mit Leerzeile)";
    gibKommaZahlEin(prompt, ergebnis);
    return 0;
}
*/